package nl.cge.domein;

import org.junit.jupiter.api.Test;

import java.math.BigDecimal;

import static org.junit.jupiter.api.Assumptions.assumeTrue;

class BankrekeningTest {

    @Test
    public void testCOnstructor() {
        System.out.println(System.getProperty("os.name"));
//        Bankrekening bankrekening = new Bankrekening("NL60393127559");
//        assumeTrue(bankrekening.getSaldo().signum() == 0);
//        assumeTrue(bankrekening.getLimiet().equals(BigDecimal.valueOf(1000)));
    }

}